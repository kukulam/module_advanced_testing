package kukulam.module.advancedtesting.task1;

// Use in tests: @Parametrized, @BeforeEach, @BeforeAll, @AfterEach, @AfterAll, assertThat, assertEquals
public interface NumberCalculator {
    int add(int a, int b);
    int minus(int a, int b);
}
